from classifier import *
from predictor import *
from dataloader import *

pretrained = False

#Create Dataset 
DataLoader = DataLoader()

#train classifier network (if not pretrained)
if(not pretrained):
    train_network(DataLoader.something)

classification 

#take novice sample
predict_novice()

